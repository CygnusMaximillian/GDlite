{
  "name": "gdlite-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client/Editor",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client/Editor",
    "dev": "npx concurrently \"npm run dev:server\" \"npm run dev:client\""
  }
}
